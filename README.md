# ИИ-Автокликер v2.0.0

Мощное браузерное расширение для автоматизации веб-действий с поддержкой искусственного интеллекта Google Gemini.

## 🌟 Особенности

### Основной функционал
- 🎬 **Запись действий** - Записывайте клики, вводы текста, скролл и другие действия
- 🤖 **ИИ-анализ** - Google Gemini анализирует ваши инструкции и преобразует их в действия
- ⚡ **Быстрое воспроизведение** - Воспроизведите записанные сценарии с переменной скоростью
- 💾 **Сохранение сценариев** - Экспортируйте и импортируйте действия в JSON формате
- 🔒 **Безопасность** - Валидация входных данных и защита от XSS

### Улучшенная архитектура
- ✅ Модульная структура кода с разделением ответственности
- ✅ Интегрированное логирование и отладка
- ✅ Обработка ошибок на всех уровнях
- ✅ Кэширование селекторов для производительности
- ✅ Множественные стратегии поиска элементов
- ✅ Асинхронное выполнение с поддержкой ES6

## 📦 Структура проекта

```
ai-autoclicker/
├── src/
│   ├── common/                 # Общие утилиты
│   │   ├── constants.js       # Глобальные константы
│   │   ├── logger.js          # Система логирования
│   │   ├── validator.js       # Валидация и санитизация
│   │   ├── storage.js         # Управление хранилищем
│   │   └── helpers.js         # Вспомогательные функции
│   ├── popup/                  # Интерфейс расширения
│   │   ├── popup.html         # HTML структура
│   │   ├── popup.css          # Стили попапа
│   │   └── popup.js           # Логика попапа
│   ├── content/                # Content script
│   │   ├── content.js         # Главный скрипт
│   │   ├── content.css        # Стили на странице
│   │   ├── ElementFinder.js   # Поиск элементов
│   │   ├── ActionRecorder.js  # Запись действий
│   │   └── ActionExecutor.js  # Выполнение действий
│   ├── background/             # Background service worker
│   │   └── background.js      # Фоновый скрипт
│   ├── settings/               # Страница настроек
│   │   ├── settings.html      # HTML настроек
│   │   ├── settings.css       # Стили настроек
│   │   └── settings.js        # Логика настроек
│   ├── ai/                     # ИИ модули
│   │   └── InstructionParser.js # Парсер инструкций
│   └── manifest.json          # Конфигурация расширения
├── build/
│   └── build.js               # Скрипт сборки
├── dist/                       # Скомпилированная версия
├── tests/                      # Тесты
├── package.json               # Зависимости NPM
├── .eslintrc.js               # ESLint конфиг
├── README.md                  # Этот файл
└── .gitignore                 # Git ignore файл
```

## 🚀 Установка и использование

### Требования
- Google Chrome 88+
- Node.js 14+ (для разработки)
- Google API Key для Gemini (опционально)

### Локальная установка

1. **Клонируйте репозиторий**
```bash
git clone https://github.com/yourusername/ai-autoclicker.git
cd ai-autoclicker
```

2. **Установите зависимости**
```bash
npm install
```

3. **Соберите расширение**
```bash
npm run build
```

4. **Загрузите в Chrome**
   - Откройте `chrome://extensions/`
   - Включите "Режим разработчика" (верхний правый угол)
   - Нажмите "Загрузить распакованное расширение"
   - Выберите папку `dist/`

5. **Сконфигурируйте API ключ** (опционально)
   - Получите ключ на [Google AI Studio](https://makersuite.google.com/app/apikey)
   - Откройте "Параметры расширения" и введите ключ

### Использование

#### Ручной режим
1. Нажмите "🔴 Запись" в попапе расширения
2. Выполняйте действия на странице (клики, ввод текста, скролл)
3. Нажмите "⏹️ Стоп" для завершения записи
4. Нажмите "▶️ Играть" для воспроизведения

#### Автоматический режим (с ИИ)
1. Переключитесь на вкладку "Автоматический"
2. Напишите инструкции на русском: *"Кликни на кнопку Войти, введи email, затем пароль, нажми отправить"*
3. Нажмите "▶️ Запустить"
4. Расширение проанализирует инструкции и выполнит действия

#### Управление сценариями
- **Экспорт**: Нажмите "📥 Экспорт" для сохранения действий в JSON
- **Импорт**: Нажмите "📤 Импорт" и выберите JSON файл
- **Скорость**: Используйте ползунок для изменения скорости воспроизведения

## 💡 Примеры инструкций для ИИ

### Авторизация
```
Кликни на кнопку "Войти", введи email "user@example.com", затем введи пароль "password123", нажми "Отправить"
```

### Заполнение формы
```
Найди поле "Имя" и введи "Иван Петров"
Выбери из выпадающего списка "Страна" значение "Россия"
Кликни на чекбокс "Согласен с условиями"
Нажми кнопку "Зарегистрироваться"
```

### Навигация и скролл
```
Прокрути страницу на 400 пиксель вниз
Кликни на ссылку "Далее"
Жди 2 секунды для загрузки страницы
Нажми кнопку "Скачать"
```

## 🔧 Для разработчиков

### Запуск в режиме разработки
```bash
npm run dev
```

Это запустит сборку и будет отслеживать изменения.

### Линтинг
```bash
npm run lint
```

### Форматирование кода
```bash
npm run format
```

### Запуск тестов
```bash
npm test
```

## 📚 API документация

### ActionRecorder

```javascript
const recorder = new ActionRecorder(elementFinder);

// Начать запись
recorder.start();

// Получить записанные действия
const actions = recorder.getActions();

// Остановить запись
recorder.stop();

// Слушать события
recorder.on('action-recorded', ({ action, count }) => {
  console.log(`Действие ${count}: ${action.type}`);
});
```

### ActionExecutor

```javascript
const executor = new ActionExecutor(elementFinder);

// Выполнить одно действие
await executor.executeAction(action);

// Выполнить последовательность
await executor.executeSequence(actions, speed = 1);

// Слушать события выполнения
executor.on('action-completed', ({ action }) => {
  console.log(`Завершено: ${action.type}`);
});

executor.on('sequence-error', ({ error }) => {
  console.error(`Ошибка: ${error}`);
});
```

### ElementFinder

```javascript
const finder = new ElementFinder();

// Найти элемент
const element = finder.find('Кнопка входа');

// Найти по селектору
const elem = finder.findBySelector('.button-primary');

// Найти по тексту
const btn = finder.findByText('Войти');

// Найти по XPath
const input = finder.findByXPath('//input[@name="username"]');

// Очистить кэш
finder.clearCache();
```

### InstructionParser

```javascript
// Парсить с использованием Gemini
const actions = await InstructionParser.parse(
  'Кликни на кнопку Войти',
  true,  // useGemini
  apiKey,
  pageContext
);

// Парсить с fallback методом
const actions = await InstructionParser.parse(
  'Кликни на кнопку Войти',
  false  // useGemini
);

// Валидировать действия
InstructionParser.validateActions(actions);

// Оптимизировать последовательность
const optimized = InstructionParser.optimizeSequence(actions);
```

### StorageManager

```javascript
// Получить значения
const data = await StorageManager.get(['key1', 'key2'], 'sync');

// Установить значения
await StorageManager.set({ key: 'value' }, 'sync');

// Получить действия
const actions = await StorageManager.getActions();

// Сохранить действия
await StorageManager.saveActions(actions);

// Получить историю выполнения
const history = await StorageManager.getExecutionHistory(50);
```

## 🔒 Безопасность

### Встроенные меры безопасности

1. **Валидация входных данных**
   - Проверка типов и диапазонов значений
   - Максимальные длины текста
   - Валидация URL и селекторов

2. **Защита от XSS**
   - Санитизация HTML при отображении
   - Использование textContent вместо innerHTML где возможно

3. **Управление разрешениями**
   - Минимальный набор разрешений в manifest
   - Запросы разрешений только при необходимости

4. **Безопасное хранилище**
   - Использование Chrome Storage API с синхронизацией
   - Обработка ошибок при доступе к хранилищу

## 🐛 Решение проблем

### Расширение не работает
1. Проверьте, что расширение включено в `chrome://extensions/`
2. Перезагрузите страницу (Ctrl+R)
3. Откройте консоль разработчика (F12) и проверьте ошибки

### ИИ не анализирует инструкции
1. Проверьте наличие API ключа в настройках
2. Нажмите "🧪 Тест" для проверки подключения
3. Убедитесь, что у вас осталась квота (1000 запросов в день)

### Элементы не находятся
1. Проверьте, что элемент виден на странице
2. Попробуйте использовать более описательное имя
3. Откройте консоль и используйте `elementFinder.find('...')` для отладки

## 📝 Форматы данных

### Формат действия

```javascript
{
  type: 'click',                    // Тип действия
  target: 'Кнопка входа',          // Текст или описание
  selector: '.button-login',       // CSS селектор
  timestamp: 1699450000000,        // Время выполнения (опционально)
  value: 'user@example.com'        // Значение (для input)
}
```

### Экспортированный JSON
```json
[
  {
    "type": "click",
    "target": "Кнопка входа",
    "selector": "button.login"
  },
  {
    "type": "input",
    "target": "Email",
    "value": "user@example.com"
  },
  {
    "type": "wait",
    "duration": 1000
  }
]
```

## 🤝 Контрибьютинг

Мы приветствуем вклады! Пожалуйста:

1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/amazing-feature`)
3. Коммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Пушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

### Рекомендации
- Следуйте существующему стилю кода
- Добавляйте JSDoc комментарии к функциям
- Пишите тесты для новых функций
- Обновляйте README при необходимости

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. [LICENSE](LICENSE) файл для деталей.

## 🙏 Благодарности

- [Google Gemini API](https://makersuite.google.com/) - для ИИ анализа
- [Chrome Extensions API](https://developer.chrome.com/docs/extensions/) - для работы с браузером

## 📞 Поддержка

- 🐛 [Баг-репорты](https://github.com/yourusername/ai-autoclicker/issues)
- 💡 [Предложение фич](https://github.com/yourusername/ai-autoclicker/discussions)
- 📧 Email: support@example.com

---

**Версия**: 2.0.0  
**Последнее обновление**: 2024  
**Автор**: Your Name
