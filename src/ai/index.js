/**
 * AI Module Exports
 */

// This is a placeholder for module exports if using a bundler
// In content scripts, these are loaded separately via manifest.json

if (typeof module !== 'undefined' && module.exports) {
  module.exports = {
    GeminiAI,
    InstructionParser,
  };
}
