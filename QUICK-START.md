# Быстрый старт для разработчиков

## Первые шаги (5 минут)

### 1. Установка
```bash
git clone https://github.com/yourusername/ai-autoclicker.git
cd ai-autoclicker
npm install
```

### 2. Сборка
```bash
npm run build
```
Расширение собирается в папку `dist/`

### 3. Загрузка в Chrome
1. Откройте `chrome://extensions/`
2. Включите "Режим разработчика" (верхний правый угол)
3. Нажмите "Загрузить распакованное расширение"
4. Выберите папку `dist/`

### 4. Тестирование
- Откройте любую веб-страницу
- Нажмите на иконку расширения
- Нажмите "🔴 Запись" и выполняйте действия
- Нажмите "⏹️ Стоп" и затем "▶️ Играть" для воспроизведения

## Разработка

### Режим watch (горячая перезагрузка)
```bash
npm run dev
```
Следит за изменениями в `src/` и автоматически пересобирает

### Проверка качества
```bash
npm run lint      # Проверка кода
npm test          # Запуск тестов
npm run format    # Форматирование
```

### Отладка
1. **Popup скрипт**: Нажмите Inspect на иконке расширения
2. **Content скрипт**: Нажмите F12 на странице, вкладка Console
3. **Background скрипт**: `chrome://extensions/` → Service Worker

## Основная архитектура

```
popup.js ← → content.js ← → background.js
                    ↓
            ActionRecorder
            ActionExecutor
            ElementFinder
                    ↓
            [Page DOM]
```

## Главные классы

### 1. ActionRecorder
Записывает клики и вводы пользователя:
```javascript
recorder.start()         // Начать запись
recorder.getActions()    // Получить действия
recorder.stop()          // Остановить
```

### 2. ActionExecutor
Выполняет записанные действия:
```javascript
executor.executeSequence(actions, speed)
executor.on('action-completed', callback)
```

### 3. ElementFinder
Находит элементы на странице:
```javascript
finder.find('Текст кнопки')
finder.findBySelector('.button-login')
finder.findByXPath('//button')
```

## Частые задачи

### Добавить новое действие
1. Добавить тип в `ACTION_TYPES` в `constants.js`
2. Добавить обработчик в `ActionRecorder.handleClick()`
3. Добавить выполнение в `ActionExecutor.executeAction()`

### Изменить UI
1. Отредактировать HTML в `popup/popup.html`
2. Изменить CSS в `popup/popup.css`
3. Добавить обработчики в `popup/popup.js`

### Добавить новый хранилище данных
1. Добавить в `StorageManager` методы для вашего типа данных
2. Использовать в других модулях через `StorageManager.get/set`

### Улучшить парсинг инструкций
1. Добавить новый метод в `InstructionParser`
2. Проверить fallback парсинг

## Отладка типичных проблем

### Расширение не срабатывает
```javascript
// В console popup'а
chrome.tabs.query({active: true, currentWindow: true}, console.log)
```

### Элемент не находится
```javascript
// В console страницы
const finder = new ElementFinder()
finder.find('Ваш текст')  // Вернёт элемент или null
finder.getCacheStats()     // Показать кэш
```

### Не сохраняются действия
```javascript
// В console popup'а
StorageManager.getActions().then(console.log)
```

## Форматы файлов

### Экспортированные действия
```json
[
  {
    "type": "click",
    "target": "Кнопка",
    "selector": ".btn"
  },
  {
    "type": "input",
    "value": "text",
    "target": "Email"
  }
]
```

## Команды Git

```bash
# Создать ветку для функции
git checkout -b feature/my-feature

# Проверить статус
git status

# Добавить файлы
git add .

# Коммитить (следуйте соглашению)
git commit -m "feat: описание функции"
git commit -m "fix: исправление проблемы"
git commit -m "docs: обновление документации"

# Запушить
git push origin feature/my-feature
```

## Соглашения по коммитам

```
feat: новая функция
fix: исправление баги
docs: документация
style: форматирование
refactor: переработка кода
perf: оптимизация
test: тесты
chore: конфиг, зависимости
```

## Помощь

- 📚 [Полная документация](README.md)
- 🤝 [Руководство контрибьютинга](CONTRIBUTING.md)
- 📝 [Изменения](CHANGELOG.md)
- 🐛 [Issues](https://github.com/yourusername/ai-autoclicker/issues)

---

**💡 Совет**: Начните с простого - просто добавьте `console.log()` в нужное место и посмотрите в консоль девтулс!
